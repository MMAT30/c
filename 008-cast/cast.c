#include <stdlib.h>
#include <stdio.h>

int main() {

    // TYPE CONVERSION
    // format: (type) expression

    // char to int
    char ch = 'A';
    printf("ch = %c\n", ch);

    int i = (int) ch; 
    printf("i = %d\n", i);
}